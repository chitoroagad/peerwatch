# Anomaly Detection for Subnets

# Setup

Populate `src/data` with `nmap` json data _can use the `jsonify()` function in `main.py`_

## Nix

```sh
nix develop
```

## Pip

```sh
pip install -r _requirements.txt
```

# Run

```sh
cd src/
python main.py
```
