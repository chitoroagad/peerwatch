- [ ] Create features out of scanned RTTs and ports then compare them across time.
- [x] Make useful peer store to have baseline of peers to compare new measurements against
- [ ] Make peer store compare incoming peer data

# Further ideas:

- look into a computer hijacking another connected computers connection to gain access to network.
- check if services running on ports are accurate. i.e. is service on port 22 actually ssh.
- document all activity
